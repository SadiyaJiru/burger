
<h1>Burgers To Eat</h1>

<div class = "row">
  <div class="col-md-7 col-left">
          <table>
<h2>Burgers on the list</h2>
<ul>

<!--loop through burgers array for each burger-->
  <form action="/{{this.id}}?_method=CREATE" method="POST">
      {{#each burgers}}
        
          <p>{{this.id}}. {{this.burger_name}}  <button type="submit" class="btn btn-warning btn-xs" id="devour">Devour</button></p>        
        
      {{/each}}
    </form>
    
</ul>
</table>
</div>
</div>



{{!-- <div class="col-md-7 col-left">
  <h2>Devoured Burgers</h2>

<!--loop through burgers array for each burger-->
{{#each burgers}}
<!--If it is devourved just display the burger name-->
{{#if devoured}}
<div> id = {{id}}.{{burger_name}} </div>
{{/if}}

</div>
</section> --}}
{{!-- 

<h2>Add a Burger</h2>
    <form class="create-form">
        <div class="form-group">
            <label for="name"></label>
            <textarea id="name" name="burger-name" rows="2" cols="40" placeholder="What Burger Are You Thinking Of?"></textarea>
        </div>
        <button type="submit">Submut</button>
    </form>     
</div> --}}
<h2>Add a Burger</h2>
<form action="/" method="POST">
	<textarea type="text" name="task"></textarea>

	<button type="submit">Submit</button>
</form>
