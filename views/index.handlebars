<h1>Eat Your Burger!</h1>



<ul>
    <h2>Burgers not eaten</h2>
 <!--loop through cats array on each cat-->
  {{#each burger}}
    {{#unless devour}}
    <!--Print the cat information ubless the cat is sleepy -->
    <!--This is a partial, 
    tiny snipets of code that can be used in other other codes-->
     <!--Instead of creating anther template we use the one from Partial-->
      {{> burgers/burgers-block devour=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers Devoured!</h2>

<ul>
  {{#each burger}}
 <!--Print the cat information if the burger is eaten -->

    {{#if devour}}
      {{> burgers/burgers-block devour=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devour">Devour?</label><br>
    <input type="radio" name="devour" value="1" checked> Devour!<br>
    <input type="radio" name="devour" value="0"> Save
  </div>

  <button type="submit">Add Burger</button>
</form>
